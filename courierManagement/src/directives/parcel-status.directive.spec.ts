import { ParcelStatusDirective } from './parcel-status.directive';

describe('ParcelStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new ParcelStatusDirective();
    expect(directive).toBeTruthy();
  });
});
